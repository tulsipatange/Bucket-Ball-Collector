import 'package:flutter/material.dart';

class Ball {
  double x;
  double y;
  double speed;
  Color color;
  double size;

  Ball({
    required this.x,
    required this.y,
    required this.speed,
    required this.color,
    required this.size,
  });
}